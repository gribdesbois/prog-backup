const elt = document.getElementById("parent");
tab=[];
elt.addEventListener('click', function(event) {
  event.stopPropagation();
  alert("document.querySelector("#article > #child-count").valueOf;});
alert('${tab[0]');